# Provision the machine you are sitting at (Linux or WSL2 Ubuntu).
#   ansible-playbook playbooks/workstation.yml -K
all:
  children:
    workstation:
      hosts:
        localhost:
          ansible_connection: local
